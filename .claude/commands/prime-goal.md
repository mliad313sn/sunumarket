---
description: Run the Prime Agent mission — review, improve, complete SunuMarket (docs/PRIME_GOAL_PROMPT.md)
---

Adopt `.claude/agents/prime.md` as your operating instructions, then execute the mission in
`docs/PRIME_GOAL_PROMPT.md` end to end: prime sequence first, then work the priority list
top-down (red gates → browser-verified golden path via the playwright-cli skill → UI
elevation via impeccable/design-taste-frontend under the DC-15 budget → backlog burn-down).
Run the full verification gate before claiming any increment done. Commit each coherent
increment with a conventional message and push to the working branch. If arguments were
given ("$ARGUMENTS"), treat them as a scoping constraint on the mission.
